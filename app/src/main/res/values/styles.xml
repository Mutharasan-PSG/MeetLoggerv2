
<resources>
<style name="Theme.MeetLoggerv2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Customize your theme here -->
    <item name="colorPrimary">#FFFFFF</item> <!-- Action bar background color -->
    <item name="colorPrimaryVariant">#FFFFFF</item>
    <item name="colorOnPrimary">#000000</item> <!-- Action bar text/icons color -->
    <item name="android:statusBarColor">#FFFFFF</item> <!-- Status bar background color -->
    <item name="android:windowLightStatusBar">true</item> <!-- Ensures status bar icons are dark -->
</style>

    <!-- res/values/styles.xml -->
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/card_slide_in</item>
        <item name="android:windowExitAnimation">@anim/card_slide_out</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="CustomBottomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <!-- Customize the animation duration or use a custom animation -->
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

</resources>